const $=s=>document.querySelector(s);const tracks=[];const prompt=$("#prompt");document.querySelectorAll("#chips button").forEach(b=>b.onclick=()=>prompt.value+=(prompt.value?", ":"")+b.textContent);$("#generate").onclick=()=>{const p=prompt.value.trim();if(!p){$("#status").textContent="Describe the music you want first.";return}const mood=$("#mood").value,len=$("#length").value,v=$("#vocals").checked;const title=p.split(/[,\.]/)[0].slice(0,36)||"New creation";tracks.unshift({title,mood,len,v});render();$("#title").textContent=title;$("#meta").textContent=mood+" • "+len+" sec • "+(v?"Vocals":"Instrumental");$("#status").textContent="Track recipe created. Connect a server-side music provider to render the audio.";};function render(){const box=$("#tracks");$("#count").textContent=tracks.length+" track"+(tracks.length===1?"":"s");box.innerHTML=tracks.length?tracks.map((t,i)=>'<div class="track"><div><strong>'+escapeHtml(t.title)+'</strong><small>'+t.mood+" • "+t.len+" sec • "+(t.v?"Vocals":"Instrumental")+'</small></div><button onclick="useTrack('+i+')">Open</button></div>').join(""):'<div class="empty">Generate a track and it will appear here.</div>'}window.useTrack=i=>{const t=tracks[i];$("#title").textContent=t.title;$("#meta").textContent=t.mood+" • "+t.len+" sec"};function escapeHtml(s){return s.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",""":"&quot;","'":"&#39;"}[c]))}